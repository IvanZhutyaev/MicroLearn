# Инструкция по запуску проекта MicroLearn

## Что уже готово ✅

- ✅ Структура проекта (папки, файлы)
- ✅ Код всех основных модулей (backend + frontend)
- ✅ Схема базы данных (Prisma)
- ✅ Docker конфигурация
- ✅ API endpoints реализованы

## Что нужно сделать для запуска 🚀

### 1. Установка зависимостей

#### Backend
```bash
cd backend
npm install
```

#### Frontend
```bash
cd frontend
npm install
```

### 2. Настройка переменных окружения

#### Backend (.env)
Создайте файл `backend/.env`:
```env
DATABASE_URL=postgresql://user:password@localhost:5432/microlearn?schema=public
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=1d
JWT_REFRESH_SECRET=your-refresh-secret-key
JWT_REFRESH_EXPIRES_IN=7d
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
```

#### Frontend (.env.local)
Создайте файл `frontend/.env.local`:
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

### 3. Настройка базы данных

```bash
cd backend
npx prisma generate
npx prisma migrate dev --name init
```

### 4. Запуск проекта

#### Вариант 1: Через Docker (рекомендуется)
```bash
# Из корневой папки проекта
docker-compose up -d
```

#### Вариант 2: Локально

**Backend:**
```bash
cd backend
npm run start:dev
```

**Frontend:**
```bash
cd frontend
npm run dev
```

### 5. Проверка работы

- Backend API: http://localhost:3001/api
- Frontend: http://localhost:3000

## Возможные проблемы и решения

### Проблема: База данных не подключается
**Решение:** Убедитесь, что PostgreSQL запущен и переменная `DATABASE_URL` правильная.

### Проблема: Redis не подключается
**Решение:** Redis нужен для кэширования. Можно временно отключить или запустить через Docker.

### Проблема: Stripe ошибки
**Решение:** Для разработки можно использовать тестовые ключи Stripe или временно отключить платежи.

## Следующие шаги после запуска

1. Создать первого пользователя (регистрация)
2. Создать тестовый курс (как teacher)
3. Протестировать основные функции

## Что еще нужно доработать (опционально)

- [ ] OAuth2 интеграция (Google/GitHub)
- [ ] Загрузка файлов на AWS S3
- [ ] Система рейтингов курсов
- [ ] Ежедневные задания
- [ ] Лидерборды
- [ ] Email отправка (для верификации)
- [ ] Unit и E2E тесты

